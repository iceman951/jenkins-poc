pipeline {
    agent any
    stages {

        stage("Initialisation") {
            steps { 
                script {
                    echo "fiest script"
                    load("load-steps/steps/step-initialisation.groovy").execute(props)
                } 
            }
        }

    }
}
