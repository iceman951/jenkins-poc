def location = "pwd".execute().text
println location

pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                // echo "${config}"
                // echo "${conf}"
                // echo "${conf.text}"
                echo 'test1'
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}