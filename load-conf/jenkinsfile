def envConfig = readFile("${WORKSPACE}/load-conf/env.conf")

pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                echo "${envConfig}"
                // echo "${conf}"
                // echo "${conf.text}"
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}