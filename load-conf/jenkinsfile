String currentDir = new File(".").getAbsolutePath()

def conf = new File("${currentDir}/pipeline.conf")
// def conf = new File('load-conf/pipeline.conf').text
pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                echo "${conf}"
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}