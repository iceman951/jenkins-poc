import java.io.File
def workspace = "${env.WORKSPACE}"
def text = new File("${workspace}/load-conf/pipeline.conf").newInputStream()
is.eachLine { 
    println it
}
is.close()

pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                echo "${text}"
                // echo "${conf}"
                // echo "${conf.text}"
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}