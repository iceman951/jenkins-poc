def location = "ls".execute().text
println location

pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                echo 'test1'
                echo "env.giturl => ${env.giturl}"
                echo "env.git2 => ${env.git2}"
                echo "env.COMPUTE_VAR => ${env.COMPUTE_VAR}"
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}
