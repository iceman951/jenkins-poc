def location = "ls".execute().text
println location

pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                echo 'test1'
                echo "${env.giturl}"
                echo "${env.git2}"
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}
