System.getProperty("user.dir");
// def conf = new File('load-conf/pipeline.conf').text
pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                echo "${conf}"
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}