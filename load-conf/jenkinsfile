node('controller') {
    //now you are on slave labeled with 'label'
    def workspace = pwd()
    //${workspace} will now contain an absolute path to job workspace on slave 
}

// def conf = new File("${workspace}/pipeline.conf")
// def conf = new File('load-conf/pipeline.conf').text
pipeline {
    agent any
    stages {
        stage ('test1') {
            steps {
                echo "${workspace}"
                // echo "${conf}"
                // echo "${conf.text}"
            }
        }
        stage ('test2') {
            steps {
                echo 'test2'
            }
        }
    }
}